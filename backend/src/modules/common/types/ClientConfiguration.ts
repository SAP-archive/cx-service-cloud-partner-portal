export type ClientConfiguration = {
  debug: boolean;
  clientIdentifier: string;
  clientSecret: string;
  clientVersion: string;
  backendClusterDomain: string;
  backendClusterPort: string;
  directoryServiceUrl: string;
};
